<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Data Deletion</title>
    <style>
      body {
        font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
        margin: 2rem;
      }
      .card {
        max-width: 720px;
        margin: 0 auto;
        padding: 1.5rem;
        border: 1px solid #e5e7eb;
        border-radius: 12px;
      }
      h1 {
        margin-top: 0;
      }
      form {
        margin-top: 1rem;
        display: grid;
        gap: 0.75rem;
      }
      input,
      button {
        padding: 0.625rem 0.75rem;
        font-size: 1rem;
      }
      button {
        cursor: pointer;
        background: #111827;
        color: white;
        border: 0;
        border-radius: 8px;
      }
      .note {
        color: #374151;
        font-size: 0.95rem;
      }
      .msg {
        margin-top: 0.5rem;
        color: #059669;
      }
      code {
        background: #f3f4f6;
        padding: 0.1rem 0.35rem;
        border-radius: 6px;
      }
    </style>
  </head>
  <body>
    <div class="card">
      <h1>Data Deletion Instructions</h1>
      <p class="note">
        To request deletion of your account and associated personal data, please
        submit your email below. You may also contact us at
        <code>support@example.com</code> if you prefer.
      </p>
      <form method="POST" action="/legal/data-deletion">
        <input
          type="email"
          name="email"
          placeholder="Enter your account email"
          required
        />
        <button type="submit">Request Deletion</button>
      </form>
      <% if (typeof message !== 'undefined') { %>
      <div class="msg"><%= message %></div>
      <% } %>
      <p class="note">
        Once processed, your account and personal data will be deleted in
        accordance with our policies. If you used a social login (Google or
        Facebook), please specify the same email linked to that provider.
      </p>
    </div>
  </body>
</html>
