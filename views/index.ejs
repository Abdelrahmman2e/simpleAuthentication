<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Welcome to Our App</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap');

    /* Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #667eea, #764ba2);
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      color: #fff;
      overflow: hidden;
      position: relative;
    }

    /* Animated background circles */
    .circle {
      position: absolute;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.1);
      animation: float 10s ease-in-out infinite;
      filter: blur(30px);
    }

    .circle:nth-child(1) {
      width: 250px;
      height: 250px;
      top: 10%;
      left: 15%;
      animation-delay: 0s;
    }

    .circle:nth-child(2) {
      width: 300px;
      height: 300px;
      bottom: 15%;
      right: 20%;
      animation-delay: 3s;
    }

    .circle:nth-child(3) {
      width: 200px;
      height: 200px;
      top: 50%;
      right: 10%;
      animation-delay: 5s;
    }

    @keyframes float {
      0%, 100% {
        transform: translateY(0) translateX(0);
      }
      50% {
        transform: translateY(-20px) translateX(20px);
      }
    }

    header {
      text-align: center;
      max-width: 450px;
      z-index: 10;
      animation: fadeInDown 1s ease forwards;
      opacity: 0;
    }

    @keyframes fadeInDown {
      to {
        opacity: 1;
        transform: translateY(0);
      }
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
    }

    header h1 {
      font-size: 3rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
      letter-spacing: 2px;
      text-shadow: 2px 2px 8px rgba(0,0,0,0.4);
    }

    header p {
      font-size: 1.2rem;
      line-height: 1.6;
      margin-bottom: 2.5rem;
      text-shadow: 1px 1px 6px rgba(0,0,0,0.3);
      color: #d1d1d1;
    }

    .btn-container {
      display: flex;
      gap: 1.2rem;
      justify-content: center;
      flex-wrap: wrap;
      z-index: 10;
      animation: fadeInUp 1s ease forwards;
      opacity: 0;
      animation-delay: 1s;
    }

    @keyframes fadeInUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
      from {
        opacity: 0;
        transform: translateY(20px);
      }
    }

    a.btn {
      position: relative;
      background-color: #fff;
      color: #764ba2;
      font-weight: 600;
      padding: 0.9rem 2.4rem;
      border-radius: 40px;
      text-decoration: none;
      font-size: 1.2rem;
      box-shadow: 0 10px 25px rgba(255,255,255,0.3);
      overflow: hidden;
      transition: color 0.3s ease;
      cursor: pointer;
      user-select: none;
      min-width: 140px;
      display: inline-flex;
      justify-content: center;
      align-items: center;
    }

    /* Ripple effect */
    a.btn::after {
      content: "";
      position: absolute;
      border-radius: 50%;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      pointer-events: none;
      transform: scale(0);
      opacity: 0.6;
      background-color: #764ba2;
      transition: transform 0.5s ease, opacity 1s ease;
      z-index: 0;
    }

    a.btn:active::after {
      transform: scale(2.5);
      opacity: 0;
      transition: 0s;
    }

    a.btn:hover {
      color: #5a3682;
    }

    footer {
      position: absolute;
      bottom: 10px;
      font-size: 0.9rem;
      color: rgba(255, 255, 255, 0.7);
      z-index: 10;
      user-select: none;
    }

    /* Responsive */
    @media (max-width: 480px) {
      header h1 {
        font-size: 2.2rem;
      }
      header p {
        font-size: 1rem;
      }
      .btn-container {
        flex-direction: column;
        gap: 1rem;
      }
      a.btn {
        min-width: 100%;
      }
    }
  </style>
</head>
<body>

  <!-- Floating background circles -->
  <div class="circle"></div>
  <div class="circle"></div>
  <div class="circle"></div>

  <header>
    <h1>Welcome to Our App</h1>
    <p>Your secure gateway to personalized services. Sign up or log in to access your profile and enjoy all features.</p>
  </header>

  <div class="btn-container">
    <a href="/auth/login" class="btn" aria-label="Login">Login</a>
    <a href="/auth/signup" class="btn" aria-label="Register">Register</a>
  </div>

  <footer>&copy; 2025 YourAppName. All rights reserved.</footer>

</body>
</html>
